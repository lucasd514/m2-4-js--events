const container = document.querySelector("#result");
const ps = document.createElement("button");

for (x = 1; x <= 5; x++) {
  const ps = document.createElement("button");
  ps.innerText = i;
  ps.container.appendChild(ps);
}
